# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / tooling
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local secrets (never commit) — secrets live in GitHub Actions only
.env
*.local.env
secrets.yaml

# NOTE: data/ is intentionally committed (CSV history + cached catalogs).
